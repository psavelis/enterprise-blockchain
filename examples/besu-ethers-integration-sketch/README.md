# Besu Ethers Integration Sketch

Uses `ethers` to encode a consortium contract call for Besu or another EVM-compatible permissioned network.

## What it demonstrates

- RPC and contract profile setup.
- Encoded transaction data for role-specific views.
- Compatibility with privacy-group aware deployment patterns.

## Run

```bash
npm run example:besu-ethers
```