# Corda REST Integration Sketch

Shows the request body and endpoint shape that a TypeScript service would send to a Corda workflow gateway.

## What it demonstrates

- Gateway profile configuration.
- Clearance request payload shaping.
- Explicit approval or rejection commands over an HTTP boundary.

## Run

```bash
npm run example:corda-rest
```