# Fabric Gateway Integration Sketch

Shows how a TypeScript service can shape Fabric Gateway configuration and proposal data before submitting to a gateway connection.

## What it demonstrates

- Connection profile inputs for MSP, peer, and certificate material.
- Shipment proposal planning with transient telemetry data.
- Payload digesting using the Fabric Gateway utility layer.

## Run

```bash
npm run example:fabric-gateway
```
