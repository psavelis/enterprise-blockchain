version: "3.8"

# Minimal local development stack for the enterprise-blockchain repository.
# Start with: docker compose up -d
# Stop with:  docker compose down -v

networks:
  enterprise-blockchain:
    name: enterprise-blockchain-net

services:
  # ── Besu dev-mode miner nodes ──────────────────────────────────────
  # RPC binds to 0.0.0.0 inside the container; host ports are published
  # on 127.0.0.1 only. CORS and host-allowlist restricted for local-dev.
  besu-validator-0:
    image: hyperledger/besu:24.12.2
    container_name: besu-validator-0
    networks:
      - enterprise-blockchain
    ports:
      - "127.0.0.1:8545:8545"
    command:
      - --network=dev
      - --rpc-http-enabled
      - --rpc-http-host=0.0.0.0
      - --rpc-http-cors-origins=http://localhost
      - --host-allowlist=localhost,127.0.0.1
      - --miner-enabled
      - --miner-coinbase=0x0000000000000000000000000000000000000000
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-sf",
          "http://localhost:8545",
          "-X",
          "POST",
          "-H",
          "Content-Type: application/json",
          "-d",
          '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}',
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

  besu-validator-1:
    image: hyperledger/besu:24.12.2
    container_name: besu-validator-1
    networks:
      - enterprise-blockchain
    ports:
      - "127.0.0.1:8546:8545"
    command:
      - --network=dev
      - --rpc-http-enabled
      - --rpc-http-host=0.0.0.0
      - --rpc-http-cors-origins=http://localhost
      - --host-allowlist=localhost,127.0.0.1
      - --miner-enabled
      - --miner-coinbase=0x0000000000000000000000000000000000000001
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-sf",
          "http://localhost:8545",
          "-X",
          "POST",
          "-H",
          "Content-Type: application/json",
          "-d",
          '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}',
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

  # ── Fabric test network (minimal) ──────────────────────────────────
  fabric-orderer:
    image: hyperledger/fabric-orderer:2.5
    container_name: fabric-orderer
    networks:
      - enterprise-blockchain
    ports:
      - "127.0.0.1:7050:7050"
    environment:
      - FABRIC_LOGGING_SPEC=INFO
      - ORDERER_GENERAL_LISTENADDRESS=0.0.0.0
      - ORDERER_GENERAL_BOOTSTRAPMETHOD=none
      - ORDERER_CHANNELPARTICIPATION_ENABLED=true
    healthcheck:
      test: ["CMD-SHELL", "echo | nc -z localhost 7050"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

  fabric-peer-org1:
    image: hyperledger/fabric-peer:2.5
    container_name: fabric-peer-org1
    networks:
      - enterprise-blockchain
    ports:
      - "127.0.0.1:7051:7051"
    environment:
      - CORE_PEER_ID=peer0.org1.example.com
      - CORE_PEER_LOCALMSPID=Org1MSP
      - FABRIC_LOGGING_SPEC=INFO
    healthcheck:
      test: ["CMD-SHELL", "echo | nc -z localhost 7051"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

  fabric-peer-org2:
    image: hyperledger/fabric-peer:2.5
    container_name: fabric-peer-org2
    networks:
      - enterprise-blockchain
    ports:
      - "127.0.0.1:7052:7051"
    environment:
      - CORE_PEER_ID=peer0.org2.example.com
      - CORE_PEER_LOCALMSPID=Org2MSP
      - FABRIC_LOGGING_SPEC=INFO
    healthcheck:
      test: ["CMD-SHELL", "echo | nc -z localhost 7051"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

  # ── Corda nodes ────────────────────────────────────────────────────
  corda-notary:
    image: corda/corda-zulu-java11-5.1:5.1
    container_name: corda-notary
    networks:
      - enterprise-blockchain
    ports:
      - "127.0.0.1:10006:10006"
    environment:
      - MY_LEGAL_NAME=O=Notary,L=London,C=GB
    healthcheck:
      test: ["CMD-SHELL", "echo | nc -z localhost 10006"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s

  corda-partya:
    image: corda/corda-zulu-java11-5.1:5.1
    container_name: corda-partya
    networks:
      - enterprise-blockchain
    ports:
      - "127.0.0.1:10007:10006"
    environment:
      - MY_LEGAL_NAME=O=PartyA,L=London,C=GB
    healthcheck:
      test: ["CMD-SHELL", "echo | nc -z localhost 10006"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s

  corda-partyb:
    image: corda/corda-zulu-java11-5.1:5.1
    container_name: corda-partyb
    networks:
      - enterprise-blockchain
    ports:
      - "127.0.0.1:10008:10006"
    environment:
      - MY_LEGAL_NAME=O=PartyB,L=London,C=GB
    healthcheck:
      test: ["CMD-SHELL", "echo | nc -z localhost 10006"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
